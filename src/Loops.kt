fun main() {
    val names = listOf("Calyse", "Matt", "Sean")
    val numbers = intArrayOf(1, 2, 3)

    for (name in names) {
        println(name)
    }
    println("----")
    for (number in numbers) {
        println(number)
    }
}